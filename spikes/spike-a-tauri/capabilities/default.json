{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike: the page needs to call the pause commands and listen for events.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
